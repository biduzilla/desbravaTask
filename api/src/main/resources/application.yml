server:
  port: 8080
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: desbravaTask
  flyway:
    depends-on: entityManagerFactory
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/desbravataskdb?allowPublicKeyRetrieval=true
    driverClassName: com.mysql.cj.jdbc.Driver
    password: bolinhaBranca19

  jpa:
    #    hibernate:
    #      ddl-auto: update
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: 'true'
        show_sql: 'true'
    show-sql: 'true'

security:
  jwt:
    key: dGVuaG8gcXVlIHBhc3NlYXIgY29tIGVsZXM=
    expiracao: '30'


